name: Deploy AI Worker to ECR

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'models/**'
      - 'docs/**'
      - 'simulation/**'
      - '*.md'
      - '.gitignore'

env:
  AWS_REGION: ap-southeast-1
  ECR_REPOSITORY: u-lens-production-ai-worker

jobs:
  deploy:
    name: Build and Deploy to ECR
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      id-token: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::919246573418:role/u-lens-production-gh-ai-worker-cicd
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          
          # Remove unnecessary tools and packages
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          
          # Clean Docker
          docker system prune -af --volumes
          
          echo "Disk space after cleanup:"
          df -h
      
      - name: Build, tag, and push image to ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Build Docker image (models not included - downloaded from S3 on EC2)
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          # Push to ECR
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
      
      - name: Trigger EC2 to pull new image (via SSM)
        run: |
          echo "Triggering EC2 instances to pull new image..."
          
          # Get running EC2 instances with Service=ai-worker OR ai-rechecker tag
          INSTANCE_IDS=$(aws ec2 describe-instances \
            --filters "Name=tag:Service,Values=ai-worker,ai-rechecker" "Name=instance-state-name,Values=running" \
            --query 'Reservations[*].Instances[*].InstanceId' \
            --output text \
            --region ${{ env.AWS_REGION }})
          
          if [ -n "$INSTANCE_IDS" ] && [ "$INSTANCE_IDS" != "None" ]; then
            echo "Found AI worker instances: $INSTANCE_IDS"
            
            # Trigger update script via SSM
            aws ssm send-command \
              --document-name "AWS-RunShellScript" \
              --targets "Key=tag:Service,Values=ai-worker,ai-rechecker" \
              --parameters 'commands=["/usr/local/bin/update-ai-worker.sh"]' \
              --comment "Auto-triggered by GitHub Actions deployment" \
              --region ${{ env.AWS_REGION }} || echo "SSM command failed - instance may not have SSM agent"
            
            echo "Update command sent to EC2 instances"
          else
            echo "No running AI worker instances found - image will be pulled on next startup"
          fi
      
      - name: Deployment Summary
        if: success()
        run: |
          echo "AI Worker image pushed to ECR successfully!"
          echo ""
          echo "Image: ${{ steps.build-image.outputs.image }}"
          echo "Repository: ${{ env.ECR_REPOSITORY }}"
          echo "Region: ${{ env.AWS_REGION }}"
          echo ""
          echo "Deployment behavior:"
          echo "  • Running EC2: Triggered immediately via SSM"
          echo "  • Stopped EC2: Will pull latest image on next startup"
