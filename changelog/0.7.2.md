# v0.7.2 — 2026-02-25

## Fixed
- POSM compliance: duplicate YAML keys for `gal_relaunch_shelf_strip_feb26` and `gal_relaunch_poster_jan26` caused second entry to silently overwrite first, breaking planned name matching
- POSM compliance: added `aliases` support so one AI class can match multiple planned names (JAN26 and FEB26 variants now both work)

## Added
- `pyproject.toml` with canonical version field
- `core.__version__` for runtime version access
- `get_all_names()` in `POSMCompliance` to resolve primary name + aliases
