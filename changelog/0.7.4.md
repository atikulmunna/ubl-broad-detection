# v0.7.4 — 2026-03-01

## Changed
- Refactored `posm_standards.yaml` to use standard/campaign names as keys with `ai_class` field — multiple standard names can now share the same AI class (e.g. Jan26/Feb26 campaign variants) without collision; eliminates aliases
- Updated `posm_compliance.py` to build reverse map from new structure; `ai_class` supports string or list (for shelftalker-based POSM items)
- Added `cap_visible_to_planned` toggle in `config.yaml` under `posm:` — when true, reported `visible_qty` is capped at `planned_qty`
- Updated `qpds_standards.yaml` for March: JUNIORS variant_threshold 50→100, Lux overall 80→90 + exclusivity enabled, PS Skin/Hair shelftalker_affects_success enabled, Hair Care overall 70→80

## Added
- New POSM items: `PEPSODENT GERMICHECK POSTER FEB26` and 6 PS shelftalker-based items (GAL/PONDS/HAIR CARE rolling stickers and pet sheets) mapped to shelftalker detection classes
- Saved `qpds_standards_feb26.yaml` for reruns of historical February data
