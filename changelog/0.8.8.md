# v0.8.8 — 2026-03-08

## Fixed
- QPDS classification OOM when many segmentation crops are present — replaced single bulk `predict()` call with chunked inference; `torch.cuda.empty_cache()` between chunks defragments reserved-but-unallocated GPU memory; on OOM, chunk size halves and retries rather than falling back to batch=1 on the full list
