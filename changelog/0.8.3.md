# v0.8.3 — 2026-03-02

## Fixed
- Fix inconsistent return values in `_detect_shelftalker_roi` causing `ValueError: not enough values to unpack (expected 3, got 2)` when no shelftalkers detected or invalid polygon created
